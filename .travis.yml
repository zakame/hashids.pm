language: perl
sudo: false
perl:
  - 5.26
  - 5.28
  - 5.30
  - dev
matrix:
  include:
    - perl: 5.30
      env: COVERAGE=1
before_install:
  - eval $(curl https://travis-perl.github.io/init) --auto
